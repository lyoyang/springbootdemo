spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test2
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    mode: HTML5
    cache: false
  devtools:
    livereload:
      enabled: true
logging:
  level:
    com.lyoyang.springbootredis.* : debug

